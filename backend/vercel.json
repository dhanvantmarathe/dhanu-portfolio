{
    "version": 2,
   {
  "builds": [
    { "src": "server.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" }
  ]
},
    "env": {
      "NODE_ENV": "production",
      "CUSTOM_VARIABLE": "value"
    }
  }
  
